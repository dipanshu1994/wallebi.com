import { ExpoToNumberPipe } from './expo-to-number.pipe';

describe('ExpoToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpoToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
